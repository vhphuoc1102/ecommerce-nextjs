enum Status {
  ACTIVE
  INACTIVE
}

enum PublishStatus {
  PUBLISH
  DRAFT
}

enum VerifyStatus {
  VERIFIED
  UNVERIFIED
}

enum NewStatus {
  NEW
  LIKE_NEW
  USED
}

enum Gender {
  MALE
  FEMALE
}

enum FileKind {
  USER_DEFAULT_AVATAR
  USER_AVATAR
  USER_COMMENT
  PRODUCT_PIC
  PRODUCT_DOC
  ALBUM_ITEM
  CATEGORY_PIC
  CATEGORY_LOGO
  BRAND_PIC
  BRAND_LOGO
}

enum FileType {
  IMAGE
  VIDEO
  AUDIO
  DOCUMENT
  OTHER
}

enum UserType {
  ADMIN
  USER
}

enum PayType {
  MOMO
  VNPAY
  ZALOPAY
  CASH
}

enum OrderStatus {
  AWAITING_PAYMENT
  AWAITING_SHIPMENT
  SHIPPING
  COMPLETED
  CLOSED
  ERROR
}

enum InputType {
  MANUAL
  LIST
}

enum RelateStatus {
  RELATE
  UNRELATE
}

enum AttributeType {
  PARAM
  SPECIFICATION
}

enum SearchType {
  TEXT
  RANGE
  NO_SEARCH
}

enum SubjectType {
  PRODUCT
  BRAND
  CATEGORY
}
