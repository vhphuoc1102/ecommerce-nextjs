enum Status {
  ACTIVE
  INACTIVE
}

enum PublishStatus {
  PUBLISH
  DRAFT
}

enum VERIFY_STATUS {
  VERIFIED
  UNVERIFIED
}

enum NewStatus {
  NEW
  LIKE_NEW
  USED
}

enum Gender {
  MALE
  FEMALE
}

enum FileKind {
  USER_DEFAULT_AVATAR
  USER_AVATAR
  USER_COMMENT
  PRODUCT
  ALBUM_ITEM
  CATEGORY_PIC
  CATEGORY_LOGO
  BRAND_PIC
  BRAND_LOGO
}

enum FileType {
  IMAGE
  VIDEO
  AUDIO
  DOCUMENT
  OTHER
}

enum UserType {
  ADMIN
  USER
}

enum PayType {
  MOMO
  VNPAY
  ZALOPAY
  CASH
}

enum OrderStatus {
  AWAITING_PAYMENT
  AWAITING_SHIPMENT
  SHIPPING
  COMPLETED
  CLOSED
  ERROR
}
